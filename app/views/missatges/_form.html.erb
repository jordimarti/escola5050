<%= form_with(model: missatge, local: true) do |form| %>
  <% if missatge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(missatge.errors.count, "error") %> prohibited this missatge from being saved:</h2>

      <ul>
        <% missatge.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :contingut %>
    <%= form.text_area :contingut %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
